//从指定位置的插入排序
void mysort(int *arr,int cur,int n)   
{  
  int i,j,tmp;
  for(i = cur;i<n;i++)  
    {  
      tmp = arr[i];
      j = i-1;
      while(j>=0&&tmp<arr[j])
	{
	  arr[j+1] = arr[j];
	  j--;   
	}  
      arr[j+1] = tmp;
    }  
}  
